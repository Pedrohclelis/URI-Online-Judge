#include <stdio.h>
 
int main() {
 
    int n;
    scanf("%i", &n);
    printf("%i ano(s)\n", n / 365);
    n %= 365;
    printf("%i mes(es)\n", n / 30);
    n %= 30;
    printf("%i dia(s)\n", n);
    
    return 0;
}
